services:
  db:
    container_name: tt-database
    image: cleanstart/sqlite3:latest
    ports:
      - "5436:5436"
    volumes:
      - ./data:/data
    environment:
      - SQLITE_DATABASE=/data/database.sqlite
    networks:
      - internalnet
networks:
  internalnet:
    driver: bridge
